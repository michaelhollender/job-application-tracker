# "v1 rules +  module boundaries" (high leverage)
“V1 Contract” section (just text) that states:
3 flows: create, list, update-status+event
2 tables only
statuses: SAVED, APPLIED, INTERVIEW, OFFER, REJECTED
endpoints list:
/applications
/applications/{id}
/aaplications/{id}/events

